# Product Capability Status

<!-- Generated by scripts/product_capability_contract.mjs. Edit config/product_capabilities.json, then run write. -->

This page is the maintained product-capability view generated from `config/product_capabilities.json`. It reports declared engineering maturity and evidence state; it does not grant distribution permission.

## Default stable flagship

**Unity + Trouble Brewing + deterministic AI** (`tb-unity-deterministic`) is the single default stable flagship.

- Entry command: `npm run unity:demo`
- Engineering maturity: **stable**
- Current evidence compliance: **noncompliant**
- Intended class: **private-development**
- Public distribution: **blocked** - Public distribution requires separate permission evidence.

## Laboratory tracks

| Track | Engineering maturity | Current evidence | Public distribution | Next action |
| --- | --- | --- | --- | --- |
| Bad Moon Rising (`bmr`) | **laboratory** | **noncompliant** | **blocked** | Close the unmet gates: `rules-fidelity`, `player-journey`, `automated-contracts`. |
| Sects & Violets (`snv`) | **laboratory** | **noncompliant** | **blocked** | Close the unmet gates: `rules-fidelity`, `player-journey`, `automated-contracts`. |
| Electron player UI (`electron-player-ui`) | **laboratory** | **noncompliant** | **blocked** | Close the unmet gates: `rules-fidelity`, `player-journey`, `automated-contracts`. |
| LocalLLM dialogue renderer (`local-llm-renderer`) | **laboratory** | **noncompliant** | **blocked** | Close the unmet gates: `rules-fidelity`, `player-journey`, `automated-contracts`. |

## Promotion gate status

Each track is evaluated against the same four gate categories. A laboratory track with all gates satisfied is only eligible for human promotion review; the generator never changes its classification.

### Unity + Trouble Brewing + deterministic AI (`tb-unity-deterministic`)

| Gate | Meaning | State | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rules fidelity | The implemented rules match the declared script behavior and known edge cases. | **unverified** | `npm run test:role-actions` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Player journey | A player can complete the intended entry-to-outcome flow for this track. | **unverified** | `npm run test:unity-demo-acceptance` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Automated contracts | Machine-checkable boundaries protect the track from regressions. | **unverified** | `npm run test:unity-viewmodel` (ci-run: **unverified**)<br>`npm run test:ai-flagship-replay` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Intended distribution | The declared use and release posture has explicit evidence; it is not inferred from engineering maturity. | **passed** | `README.md` (evergreen: **passed**) | Keep the declared evidence current. |

### Bad Moon Rising (`bmr`)

| Gate | Meaning | State | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rules fidelity | The implemented rules match the declared script behavior and known edge cases. | **missing** | No evidence declared | Declare reviewable evidence in the capability contract. |
| Player journey | A player can complete the intended entry-to-outcome flow for this track. | **unverified** | `npm run test:full-game-loop` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Automated contracts | Machine-checkable boundaries protect the track from regressions. | **unverified** | `npm run test:role-actions` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Intended distribution | The declared use and release posture has explicit evidence; it is not inferred from engineering maturity. | **passed** | `README.md` (evergreen: **passed**) | Keep the declared evidence current. |

### Sects & Violets (`snv`)

| Gate | Meaning | State | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rules fidelity | The implemented rules match the declared script behavior and known edge cases. | **missing** | No evidence declared | Declare reviewable evidence in the capability contract. |
| Player journey | A player can complete the intended entry-to-outcome flow for this track. | **unverified** | `npm run test:full-game-loop` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Automated contracts | Machine-checkable boundaries protect the track from regressions. | **unverified** | `npm run test:role-actions` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Intended distribution | The declared use and release posture has explicit evidence; it is not inferred from engineering maturity. | **passed** | `README.md` (evergreen: **passed**) | Keep the declared evidence current. |

### Electron player UI (`electron-player-ui`)

| Gate | Meaning | State | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rules fidelity | The implemented rules match the declared script behavior and known edge cases. | **unverified** | `npm run test:role-actions` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Player journey | A player can complete the intended entry-to-outcome flow for this track. | **missing** | No evidence declared | Declare reviewable evidence in the capability contract. |
| Automated contracts | Machine-checkable boundaries protect the track from regressions. | **unverified** | `npm run test:electron-build` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Intended distribution | The declared use and release posture has explicit evidence; it is not inferred from engineering maturity. | **passed** | `README.md` (evergreen: **passed**) | Keep the declared evidence current. |

### LocalLLM dialogue renderer (`local-llm-renderer`)

| Gate | Meaning | State | Evidence | Next action |
| --- | --- | --- | --- | --- |
| Rules fidelity | The implemented rules match the declared script behavior and known edge cases. | **missing** | No evidence declared | Declare reviewable evidence in the capability contract. |
| Player journey | A player can complete the intended entry-to-outcome flow for this track. | **missing** | No evidence declared | Declare reviewable evidence in the capability contract. |
| Automated contracts | Machine-checkable boundaries protect the track from regressions. | **unverified** | `npm run test:ai-llm-renderer` (ci-run: **unverified**) | Run the declared command in CI and supply its revision- and contract-bound result. |
| Intended distribution | The declared use and release posture has explicit evidence; it is not inferred from engineering maturity. | **passed** | `README.md` (evergreen: **passed**) | Keep the declared evidence current. |

## Evidence claim boundaries

These boundaries are declared by each evidence entry in `config/product_capabilities.json`; passing evidence must not be read as a broader product claim.

### Fixed-seed replay-invariant evidence

- Track and gate: Unity + Trouble Brewing + deterministic AI (`tb-unity-deterministic`) / `automated-contracts`
- Evidence command: `npm run test:ai-flagship-replay`
- Certifies only: completion; deterministic identity; legality; bounded v1 information safety; repetition; cross-day stance continuity; nomination justification; stance-to-vote alignment.
- Does not certify: general natural-language quality; known sentence-density or scripted-future-action warning families; behavior/generalization on unseen seeds.

## Engineering maturity and distribution

Engineering maturity is independent of public distribution permission. `stable` means the project maintains that engineering path; the separate `default` flag selects the flagship. Neither state means a binary or third-party asset is cleared for release. Current evaluated distribution summary: 5 blocked; 0 eligible for public-release review.

The repository's MIT license does not by itself relicense third-party names, rules, trademarks, visual assets, fonts, audio, models, or datasets.

## Provenance

- Contract hash: `sha256:7fc8792f4f7fafebf2fd2d3d0af8b6f00a1135d2fb64e048258139c727e56647`
- Classification authority: `config/product_capabilities.json`
- Generated by `scripts/product_capability_contract.mjs`
- Generator schema: `botc-product-capability-evaluation@1`
- Evidence freshness: `ci-run` requires an ephemeral result bound to the current revision and contract hash; `expires` requires an explicit as-of date; `evergreen` proves only structural presence.
- Tracked status is rendered without ephemeral CI results or local clock/mtime input, so missing runtime evidence is shown deterministically as `unverified` or `missing`.
